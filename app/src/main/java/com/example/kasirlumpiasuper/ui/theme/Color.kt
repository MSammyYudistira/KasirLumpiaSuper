package com.example.kasirlumpiasuper.ui.theme

import androidx.compose.ui.graphics.Color

val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

// Primary Color
val Primary = Color(0xFF127AF1)         // Warna utama
val PrimaryBold = Color(0xFF05318B)     // Warna utama versi gelap/tegas

// Secondary Color
val Secondary = Color(0xFFCFEEFE)       // Warna sekunder, biasanya untuk aksen ringan atau background sekunder

// Surface & Background
val Surface = Color(0xFFFFFFFF)         // Warna permukaan utama (card, sheet, dll)
val Background = Color(0xFFF9FAFB)      // Warna layar kosong, bisa digunakan sebagai background utama

// Text
val OnSurface = Color(0xFF2F353E)       // Warna teks utama (tebal)
val OnSurfaceVariant = Color(0xFF4B5563) // Warna teks sekunder / isi biasa
val OnBackground = OnSurface             // (opsional jika warnanya sama dengan OnSurface)
val HintText = Color(0xFFCACACA)         // Untuk placeholder/hint

// Border & Stroke
val Outline = Color(0xFFF5F6F7)         // Warna garis pemisah / border ringan

// Success / Positive Feedback
val Success = Color(0xFF22C55E)         // Warna hijau untuk validasi benar atau sukses
